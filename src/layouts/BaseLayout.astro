---
// Learn about using Astro layouts:
// https://docs.astro.build/en/basics/layouts/

import { getImage } from 'astro:assets'
import Footer from '../components/Footer.astro'
// Component Imports
import MainHead from '../components/MainHead.astro'
import Nav from '../components/Nav.astro'
import simonstownImg from '../images/simonstown-jagonzalr.jpg'
import portoImg from '../images/porto-jagonzalr.jpg'
import mexicoImg from '../images/mexico-jagonzalr.jpg'

interface Props {
  title?: string | undefined
  description?: string | undefined
}

const { title, description } = Astro.props

const bgMain800 = await getImage({
  src: simonstownImg,
  format: 'webp',
  width: 800
})
const bgMain1440 = await getImage({
  src: simonstownImg,
  format: 'webp',
  width: 1440
})

const bgFeatured800 = await getImage({
  src: mexicoImg,
  format: 'webp',
  width: 800
})
const bgFeatured1440 = await getImage({
  src: mexicoImg,
  format: 'webp',
  width: 1440
})

const bgProjects800 = await getImage({
  src: portoImg,
  format: 'webp',
  width: 800
})
const bgProjects1440 = await getImage({
  src: portoImg,
  format: 'webp',
  width: 1440
})
---

<html lang='en'>
  <head>
    <MainHead title={title} description={description} />
  </head>
  <body>
    <div class='stack backgrounds'>
      <Nav />
      <slot />
      <Footer />
    </div>

    <script>
      // Add "loaded" class once the document has completely loaded.
      addEventListener('load', () =>
        document.documentElement.classList.add('loaded')
      )
    </script>

    <style
      define:vars={{
        bgMain800: `url(${bgMain800.src})`,
        bgMain1440: `url(${bgMain1440.src})`,
        bgFeatured800: `url(${bgFeatured800.src})`,
        bgFeatured1440: `url(${bgFeatured1440.src})`,
        bgProjects800: `url(${bgProjects800.src})`,
        bgProjects1440: `url(${bgProjects1440.src})`
      }}
    >
      :root {
        --bg-image-main: var(--bgMain800);
        --bg-image-featured: var(--bgFeatured800);
        --bg-image-projects: var(--bgProjects800);
        --bg-scale: 1.68;
        --bg-gradient-size: calc(var(--bg-scale) * 100%);
      }

      @media (min-width: 50em) {
        :root {
          --bg-scale: 1;
          --bg-image-main: var(--bgMain1440);
          --bg-image-featured: var(--bgFeatured1440);
          --bg-image-projects: var(--bgProjects1440);
        }
      }

      .backgrounds {
        min-height: 100%;
        isolation: isolate;
        background:
          var(--bg-gradient-size) no-repeat,
          var(--gray-999);
        background-blend-mode: normal, normal;
      }

      @media (forced-colors: active) {
        /* Deactivate custom backgrounds for high contrast users. */
        .backgrounds {
          background: none;
          background-blend-mode: none;
          --bg-gradient-size: none;
        }
      }
    </style>
  </body>
</html>
